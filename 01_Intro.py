import pandas as pd
#A series is a data struture in pandas  that holds anarray of information along with a named index
# The named indx differnetiates this from a simple Numpy array
#Formal Definition ---> One-dimensional ndarray with axis labels

# pandas series adds on a labeled index to an array
# series is a numpy array hoding index but also holding inner labeled index(named index)
# tabular data structure in pandas is called 'Data Frame'
